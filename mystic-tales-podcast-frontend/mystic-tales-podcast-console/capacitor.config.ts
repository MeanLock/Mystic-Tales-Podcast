import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mystic-tales-podcast-console',
  webDir: 'build'
};

export default config;
